<% @questions.each do |question| %>
  <ul>
    <%= question.title %> | <%= link_to "Edit", edit_question_path(question) %> | <%= link_to "Delete", question_path(question), data: { confirm: "Are you sure?", method: "delete" } %>
    <br>
    <li><%= question.optionA %> <%= question.optionB %></li>
  </ul>
<% end %>
